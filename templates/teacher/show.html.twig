{% extends 'base.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block body %}
    <h1>Teacher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ teacher.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ teacher.roles ? teacher.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ teacher.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ teacher.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ teacher.prenom }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ teacher.location }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ teacher.birthday }}</td>
            </tr>
            <tr>
                <th>Biographie</th>
                <td>{{ teacher.biographie }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ teacher.gender }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ teacher.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_index') }}">back to list</a>

    <a href="{{ path('app_teacher_edit', {'id': teacher.id}) }}">edit</a>

    {{ include('teacher/_delete_form.html.twig') }}
{% endblock %}
