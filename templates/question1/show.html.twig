{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Nom_question</th>
                <td>{{ question.nomQuestion }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ question.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_question1_index') }}">back to list</a>

    <a href="{{ path('app_question1_edit', {'id': question.id}) }}">edit</a>

    {{ include('question1/_delete_form.html.twig') }}
{% endblock %}
